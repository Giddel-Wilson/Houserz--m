generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Copy the rest of the schema from the main schema.prisma file
// This will be used for production deployments
